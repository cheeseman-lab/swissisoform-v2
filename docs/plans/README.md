# Plans

Feature / integration plans. Each plan is written **after** the relevant
current-state scoping doc in `docs/architecture/` exists, and references it.

See "Working Convention: Scope Before Plan" in the repo `CLAUDE.md`.

- Current state ("what is") → `docs/architecture/`
- Proposed changes ("what we want") → here
